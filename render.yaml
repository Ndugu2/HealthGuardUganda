services:
  - type: web
    name: healthguard-backend
    env: node
    buildCommand: npm install && npm run build
    startCommand: npx serve dist
    envVars:
      - key: NODE_VERSION
        value: 20.x